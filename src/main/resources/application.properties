# Base application settings
base.url=https://the-internet.herokuapp.com
browser=chrome

# Test credentials (use environment variables or CI/CD secrets for real passwords)
credentials.username=tomsmith
credentials.password=${TEST_PASSWORD:SuperSecretPassword!}